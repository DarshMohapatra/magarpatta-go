'use client';

// Legacy Firebase verify step — the new OTP flow is inline on /signin.
// Parent page.tsx redirects before this renders; stub retained so any
// stale import resolves.
export function VerifyClient() {
  return null;
}
